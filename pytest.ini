[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers --tb=short --timeout=10
markers =
    slow: performance / scale tests (opt in with -m slow)
    scenario: qualitative behavior tests
